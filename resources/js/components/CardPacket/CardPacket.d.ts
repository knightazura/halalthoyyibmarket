import * as React from "react";
import { IPacket } from "../../../types";
interface Props {
    packet?: IPacket;
    loading?: false;
}
declare const CardPacket: React.FC<Props>;
export default CardPacket;
